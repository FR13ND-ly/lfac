class Point {
    let x: int;
    let y: int;
}

let p: Point;
let sum: int;
let i: int;

function add(a: int, b: int): int {
    let result: int;
    result = a + b;
}

main {
    p = new Point();
    p.x = 10;
    p.y = 20;
    
    sum = p.x + p.y * 2 - 5 / 1;
    
    Print(sum);
    
    if (sum > 40) {
        Print(true);
    }
    
    i = 0;
    while (i < 3) {
        Print(i);
        i = i + 1;
    }
}